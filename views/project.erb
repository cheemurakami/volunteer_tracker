<h3>Welcome to our project "<%= @project.title %>" page!</h3>

<h4> Volunteers in this project </h4>
<% if @project.volunteers %>
  <ul>
    <% @project.volunteers.each do | volunteer |%>
    <li><a href="/projects/<%= @project.id %>/volunteers/<%= volunteer.id %>"> <%= volunteer.name %></a></li>
    <% end %>
  </ul>
<% else %>
  <p> There are no volunteers listed for this project yet. </p>
<% end %>

<h4> Add a Volunteer in this project </h4>

<form action="/projects/<%= @project.id %>/edit" method="post">
  <div class="form-group">
    <label for="title">Add Name</label>
    <input id="v" type="text" name="name" class="form-control">
  </div>
  <button type="submit" class="btn btn-info">Add Volunteer</button>
</form>

<h4> Edit this project? <button class="btn btn-info"><a href="/projects/<%= @project.id %>/edit"> Edit Project</a></button>

<br>
<br>
<button class="btn btn-link"><a href="/">Home</a></button>