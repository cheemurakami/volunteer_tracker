<h3>Current Projects</h3>

<% if @projects.any? %>
  <ul>
    <% @projects.each do |project| %>
    <li><a href="/projects/<%= project.id %>"><%= project.title %></a></li>
  <% end %>
  </ul>
  <%# <p><a href="/projects?sorted=true">Sort by alphabetically</a></p> %>
<% else %>
  <p>There are currently no projects to display.</p>
<% end %>
